import numpy as np
import matplotlib.pyplot as plt

t = np.arange(-10, 10, 0.1)
plt.plot(t, t*t)
plt.show()

# pour x au carré, on peut écrire x*x ou x**2

#plutôt que multiplier les fichiers sur vsc, utilise run selection ! Et n'oublie pas #%% pour lancer une partie du code


